# 示例环境变量（复制为 .env 并填写真实值）

# 数据库：优先使用 DATABASE_URL（SQLAlchemy 格式），或使用 SQLITE_FILE 指定 sqlite 文件路径
# 例：DATABASE_URL=sqlite:////var/www/dlut-aigc-project/data/data.db
DATABASE_URL=
# 或者单独指定 sqlite 文件路径（优先级低于 DATABASE_URL）
SQLITE_FILE=

# JWT 密钥（生产环境必须替换为强随机字符串）
JWT_SECRET_KEY=change-me-in-prod

# 腾讯云账户（用于混元3D生成）
TENCENT_SECRET_ID=
TENCENT_SECRET_KEY=

# SiliconFlow（可选，用于文本增强 deepseek-r1）
SILICONFLOW_API_KEY=

# 存储（local 或 cos）。默认 local 会把文件保存到容器/主机的 /data 目录
STORAGE_PROVIDER=local
LOCAL_STORAGE_DIR=/data

# 如果使用 COS，填写下列变量并设置 STORAGE_PROVIDER=cos
COS_SECRET_ID=
COS_SECRET_KEY=
COS_REGION=ap-guangzhou
COS_BUCKET=

# 前端 origin（可选），用于限制 CORS，仅允许此 origin 访问后端
FRONTEND_ORIGIN=http://localhost:5173

# 其他可选：自定义 Flask 运行端口等
FLASK_RUN_PORT=5000

# 注意：把此文件复制为 .env 并填写真实值，切勿将 .env 中的密钥提交到公共仓库。
